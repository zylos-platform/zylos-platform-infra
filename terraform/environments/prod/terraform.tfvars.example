# Copy this to terraform.tfvars and adjust per local needs.
# DO NOT commit terraform.tfvars (it's gitignored).

aws_region         = "us-east-1"
project            = "zylos"
environment        = "prod"
vpc_cidr           = "10.20.0.0/16"
kubernetes_version = "1.33"
domain_name        = "zylos.app"
